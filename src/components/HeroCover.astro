---
import type { ComponentProps } from "astro/types";
import { Image } from "@datocms/astro";

type Props = {
  src: ComponentProps<typeof Image>["data"];
};

const { src } = Astro.props;
---

<Image
  data={src}
  priority
  sizes="(min-width: 800px) 600px, 100vw"
  imgStyle={{
    width: "100%",
    height: "100%",
    objectFit: "cover",
    maxWidth: "none",
  }}
/>
