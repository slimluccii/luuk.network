<div class="page-header">
  <slot />
</div>

<style>
  .page-header {
    padding-inline: var(--spacing-size-l);
    padding-block: calc(var(--ln-header-height) + var(--spacing-size-l)) 0px;
  }
</style>
